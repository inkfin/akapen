# 环境配置 / 本地状态
.env
.env.local

# Python venv（uv 默认放在 .venv/）
.venv/
venv/

# Python 缓存
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 运行时数据：含 SQLite / uploads / logs / records / settings.json
data/

# 历史一次性测试产物（OCR / 批改临时输出）
gemini-output/
qwen-output/

# 测试用图片集（不在仓库里追踪，避免几 MB 的 jpg 进 git）
dataset/

# IDE / OS
.DS_Store
.idea/
.vscode/
.cursor/

# Docker 本地构建产物
docker-bake.hcl.local
