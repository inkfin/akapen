# Next.js
.next/
out/
build/

# Node
node_modules/
*.log
npm-debug.log*

# 运行时数据（与 ../data/ 同思路：bind mount 上来的，不进 git）
data/

# Prisma 本地开发数据库（`prisma migrate dev` 临时跑出来的）；
# 生产 SQLite 走 web/data/web.db，由 docker 挂载并由 entrypoint 自己迁移。
prisma/dev.db
prisma/dev.db-journal

# 环境
.env
.env.local
.env.production

# IDE / OS
.DS_Store
.vscode/
.idea/

# TypeScript
*.tsbuildinfo
next-env.d.ts
